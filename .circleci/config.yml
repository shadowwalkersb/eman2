version: 2

jobs:
  build_recipe:
    docker:
      - image: circleci/buildpack-deps:16.04
    steps:
      - run: bash -c "echo"

workflows:
  version: 2
  build_recipe_and_no_recipe:
    jobs:
      - build_recipe
