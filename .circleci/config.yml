version: 2

jobs:
  build_no_recipe:
    docker:
      - image: cryoem/eman-deps:18-boost166-circleci18
    steps:
      - checkout
      - run: echo 'Skipping...'

  build_recipe:
    docker:
      - image: cryoem/eman-deps:18-boost166-circleci18
    steps:
      - checkout
      - run: echo 'Skipping...'

workflows:
  version: 2
  build_recipe_and_no_recipe:
    jobs:
      - build_no_recipe
      - build_recipe
